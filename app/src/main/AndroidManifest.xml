<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ss.lkct">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".GalleryActivity"
            android:label="@string/gallery"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".CoursesActivity"
            android:label="@string/courses"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".FacilitiesActivity"
            android:label="@string/facilities"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".FacultyActivity"
            android:label="@string/faculty"
            android:parentActivityName=".MainActivity" />


        <activity
            android:name=".BEActivity"
            android:label="@string/b_e"
            android:parentActivityName=".CoursesActivity" />

        <activity
            android:name=".MtechActivity"
            android:label="@string/m_tech"
            android:parentActivityName=".CoursesActivity" />

        <activity
            android:name=".MBAActivity"
            android:label="@string/m_b_a"
            android:parentActivityName=".CoursesActivity" />


        <activity
            android:name=".ContactActivity"
            android:label="@string/contact"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".AboutActivity"
            android:label="@string/about"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".DevelopersActivity"
            android:label="@string/developers"
            android:parentActivityName=".MainActivity"></activity>

    </application>

</manifest>